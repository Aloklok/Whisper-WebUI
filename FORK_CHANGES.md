# Whisper-WebUI Fork 更改日志 (2026-03-12)

本项目基于原版进行了针对 **中文播客转写** 和 **中端显卡 (GTX 1660 Ti 6GB)** 的深度定制与优化。

## 🚀 新增功能 (New Features)

### 1. 小宇宙播客直接下载集成
- **功能说明**：在 [文件] 标签页新增“Podcast Link (小宇宙)”输入框。
- **技术实现**：
    - 新增专用模块 `modules/utils/podcast_manager.py`。
    - 支持解析 `xiaoyuzhoufm.com` 链接，自动爬取原始音频地址并下载。
    - **极速缓存**：内置检查机制，已下载过的节目再次点击下载可秒速回显，不再重复下载。
    - **智能压缩**：下载后自动调用 `ffmpeg` 转制为极小体积且高度兼容的 `.mp3` 格式，避免之前的庞大 `.wav` 文件。

- **一键清理音频缓存 (NEW)**：提供独立脚本 `clean_temp.py`。支持一键清空临时文件目录及播客音频缓存，确保磁盘空间不被堆积文件占用。该按钮已移至界面最底部以保持界面整洁。
- **UI 布局优化 (NEW)**：将“一键清理”按钮从 AI 面板移至页面底部，并优化了其视觉样式，使其与系统自带的 API 按钮风格一致。
- **Qwen3-ASR 顶级方言支持与适配 (UPDATED)**：
    - **全量显示**：在所有推理模式（Faster-Whisper, OpenAI Whisper, Insanely Fast）的下拉框中均已注入 Qwen 模型。
    - **智能路由**：实现“选中即用”，只要在下拉框中选择 Qwen，系统会自动路由至极速 Transformers 后端处理。
    - **全量补全**：自动处理 Qwen 模型的 `.py` 脚本文件与 `merges.txt` 缺失问题，确保 Tokenizer 正常初始化。
    - **输入适配**：解决了 Qwen 无法直接处理 Numpy 矩阵的问题，实现自动临时 WAV 中转，确保 VAD/BGM 分离效果完美传递。
    - **方言最强**：支持 52 种语言/方言（含 22 种中文方言）。对于 6GB 显存显卡，强烈建议仅在转录重要内容时开启（OOM 风险预警）。

### 2. LLM 自动化洗稿/总结 (UPDATED)
- **极简集成**：在 UI 显著位置增加“✨ AI 一键整理”面板。
- **配置持久化**：支持在 `LLM Settings` 中保存 API Base/Key 等配置到本地文件。
- **OpenAI 兼容**：完美适配硅基流动 (SiliconFlow) 等主流 API 厂商。
- **超长文本并行加速 (NEW)**：放弃低效的串行排队模式，引入 `ThreadPoolExecutor` 并行架构。即使针对长达数万字的播客稿，系统也会自动分段并同步调用多路 API，处理速度提升 3-5 倍。
- **智能指令解耦 (UPDATED)**：重构了 Prompt 注入逻辑，通过物理界定符（Delimiter）彻底隔离“任务指令”与“转录正文”。解决了 Qwen 等模型偶尔会回显指令或任务说明的问题，确保输出结果纯净。
- **自动前缀清理 (NEW)**：增加了对模型输出的后处理过滤，自动识别并剔除如“待整理文本：”或自定义界定符等冗余内容。
- **隐形上下文衔接 (NEW)**：优化了分段间的背景参考格式，使其对模型更透明，既保证了衔接处的逻辑连贯，又避免了 AI 重复输出背景文字。
- **上下文感知对齐 (UPDATED)**：在并行模式下依然支持原文上下文传递，确保分段衔接处逻辑连贯。
- **容错与稳定性增强 (UPDATED)**：针对 LLM API 响应不稳定的情况，实现了极其防御性的解析逻辑与分段错误回退机制。
- **手动输入/外部文本润色 (NEW)**：支持直接在“输出”框粘贴文本进行整理。
- **实时进度反馈 (NEW)**：实时显示“并行整理中：已完成 X/N 段...”进度。
- **对话式预览 UI (NEW)**：放弃传统的纯文本框，改用精美的聊天流布局。包含彩色头像、发言人姓名标识、以及优化后的行高排版。
- **智能话题吸顶滚动 (NEW)**：话题标签在滚动时自动吸附在预览窗口顶部，且支持多话题块自动切换，极大提升长文阅读体验。
- **三格式一键导出 (UPDATED)**：支持同时导出纯文本 `.txt`、美化版 `.html` 和专业访谈笔记 `.pdf`。PDF 采用微软雅黑字体，完美还原预览排版。
- **深度思考开关 (NEW)**：在 LLM Settings 中增加“开启深度思考”开关，开启后自动优化推理参数（降低 Temperature），引导模型进行更严谨的整理。
- **多路并发整理 (UPDATED)**：通过并行 API 调用与智能分段逻辑（3500 字符/段），极大缩短了长文整理的等待时间，并解决了长文截断问题。

### 3. UI 交互感官增强 (NEW)
- **动态状态标签**：为“背景音乐消除”、“话音检测”、“说话人分离”增加动态标题标签。无需展开面板，即可通过标题结尾的 `(已开启)` 或 `(已关闭)` 确认当前状态。
- **按钮布局精调**：将“一键清理”移入 AI 面板，统一视觉风格；同时将“AI 一键整理”面板移至文件选项卡最底部，确保用户在获取转录结果后再进行后续润色。

## 🛠 稳定性与 Bug 修复 (Fixes)

### 1. 存储空间优化
- **缓存重定向**：将 Gradio 和系统的临时文件目录强制修改为项目根目录下的 `temp/`。
- **目的**：彻底解决原版默认使用系统 C 盘缓存，导致转写长音频时 C 盘空间爆满 (No space left on device) 的问题。

### 2. PyTorch 2.6+ 适配 (Diarization 终极修复)
- **全局加载补丁**：在 `app.py` 中通过 Monkey Patch 方式恢复了 `torch.load` 对受信任模型（如 Pyannote）的兼容性。
- **效果**：彻底解决了 PyTorch 2.6.0 以后由于“仅限权重加载”导致说话人分离模型无法读取内部复杂对象（如 `Specifications`, `Problem`）而崩溃的问题。

### 3. 注意力机制自动降级 (SDPA)
- **状态**：经过硬件检测，GTX 1660 Ti 不支持 Flash Attention 2 (需 Ampere 架构)。
- **优化**：项目已自动适配为 **SDPA (Scaled Dot Product Attention)** 路径，这是该代显卡能跑出的最高效并发计算模式。

## ⚡ 性能与参数优化 (Optimizations)

针对 **GTX 1660 Ti (6GB VRAM)** 打造的默认参数组合，确保在不崩溃的前提下获得最快速度：

- **核心模型**：默认改为 `large-v3-turbo`（速度提升 ~8 倍，精度媲美 large-v2）。
- **计算精度**：默认改为 `int8_float16` (性能甜点，在 1660 Ti 上比纯 int8 或 float16 更高效且精准)。
- **批处理并发**：全面集成 `BatchedInferencePipeline`，并将 `batch_size` 优化为 `12`。
- **搜索宽度**：`beam_size` 默认调整为 `4` (兼顾精度与速度，配合 turbo 模型实现毫秒级响应)。
- **VAD (语音检测) 精调**：
    - 默认开启 `vad_filter` 以剪掉长音频中的静音和废话。
    - 设定 `speech_pad_ms: 200` 和 `min_silence_duration_ms: 300`，完美适配播客快节奏对话。
- **智能排版 (NEW)**：实现同说话人片段合并算法。转录后自动将同一说话人的细碎句子（几秒）合并为逻辑连贯的整段（默认最大 30s），极大地提升了笔记的可读性。
- **默认导出格式**：应用户阅读习惯，默认设为 `txt`。
- **通用初始提示词 (Initial Prompt)**：固化了高质量的播客预设，解决了中英混排、标点和拼写不一致的顽疾。
- **投机采样加速支持**：针对 `insanely-fast-whisper` 进行了二次开发。
    - **注意**：由于需要同时加载两个模型，建议显存 **12GB+** 使用。在 6GB/8GB 显卡上开启需谨慎，防止显存溢出。

## 🔮 未来计划 (Roadmap / TODO)

### 1. 投机采样加速支持
- **当前状态**：针对 `insanely-fast-whisper` 已有基础框架。
- **计划**：优化双模型载入逻辑，引入更精细的显存池管理，使 8GB 显存也能跑起 Assistant Generation。

## 📦 依赖说明
- 新功能使用了 Python 内置库和 `requests`, `PyYAML`。
- 格式转换依赖系统中已安装的 `ffmpeg`。
